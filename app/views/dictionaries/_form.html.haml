= form_with model: @dict do |f|
  .form-field
    = f.label :source_id, "Source language"
    = f.select :source_id, @lang_select
  .form-field
    = f.label :target_id, "Target language"
    = f.select :target_id, @lang_select
  .form-action
    = f.submit @submit
